namespace JetBrains.ReSharper.Plugins.Spring.PascalParser
{
    public static class CombinatorUtils
    {
        // public static Parser<Lexeme, Lexeme> CreateToken(LexemeKind kind, string pattern)
        // {
        //     return Parser<Lexeme>.Token(token =>
        //         token.Kind == kind &&
        //         token.Text == pattern
        //     );
        // }
        //
        // public static Parser<Lexeme, Lexeme> CreateTokenOr(LexemeKind kind, string pattern1, string pattern2)
        // {
        //     return Parser<Lexeme>.Token(token =>
        //         token.Kind == kind &&
        //         (token.Text == pattern1 || token.Text == pattern2));
        // }
    }
}